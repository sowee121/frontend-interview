---
description: 本地开发服务：复用已有 Vite，禁止重复起多个实例
alwaysApply: true
---

# 本地开发服务（Vite）

本项目 `pnpm run dev` 为 Vite，题库与样式改动均支持热更新，**不要为每次浏览器抽查重新起服务**。

## 起服务前必做

1. 先检查本机是否已有可用的开发服务（常见端口 `5174`–`5180`，或 `lsof -nP -iTCP -sTCP:LISTEN` 中带 `vite` / 本仓库路径的 `node`）。
2. **已有且能打开页面（HTTP 200）→ 直接用该 URL**，不要再执行 `pnpm run dev`。
3. **没有可用服务 → 才启动一个**；启动后记下实际端口，后续抽查复用。

## 禁止

- 端口被占用时再开第二个、第三个实例「换端口继续」
- 抽查结束不主动清掉「自己刚起的重复实例」却留下多个监听
- 为验证展示名 / 题量等热更新即可覆盖的事反复 `pnpm run dev`

## 清理

若发现多个本仓库 Vite 同时监听：保留一个可用的，结束其余旧进程；不要无故全杀用户自己正在用的那一个。
